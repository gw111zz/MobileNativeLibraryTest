#sudo: required

os:
 - linux
 - osx

language: cpp

compiler:
 - gcc
 - clang

before_script:
 - cmake .

script: 
 - make

addons:
 apt:
  sources:
   - george-edison55-precise-backports
  packages:
   - cmake
   - cmake-data
   - extra-cmake-modules
